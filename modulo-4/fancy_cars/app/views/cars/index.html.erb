<h1>Listing cars</h1>

<table>
  <thead>
    <tr>
      <th>Company</th>
      <th>Color</th>
      <th>Year</th>
      <th>Price</th>
      <th>Birth date</th>
      <th>Model</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @cars.each do |car| %>
      <tr>
        <td><%= car.company %></td>
        <td><%= car.color %></td>
        <td><%= car.year %></td>
        <td><%= car.price %></td>
        <td><%= car.birth_date %></td>
        <td><%= car.model %></td>
        <td><%= link_to 'Show', car %></td>
        <td><%= link_to 'Edit', edit_car_path(car) %></td>
        <td><%= link_to 'Destroy', car, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Car', new_car_path %>
